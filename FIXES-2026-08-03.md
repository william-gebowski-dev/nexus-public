# Fixes aplicados em 2026-08-03

## O que mudou (no disco)

| Arquivo | Tipo | Fix |
|---|---|---|
| `src/lib/ai-enums.ts` | NOVO | Single source truth: `AI_PERIODS`, `AI_METRICS`, guards `isPeriod`/`isMetric` |
| `src/lib/schemas.ts` | MOD | Zod enums derivados de `ai-enums.ts`; `timezone` derivado de `ROUTINE_TIMEZONE` |
| `src/services/nexus-api.ts` | MOD | `resolveDataMode()` defensivo — fallback "mock" quando `import.meta.env` undefined (fix vitest) |
| `scripts/system-status-collector.ts` | MOD | `collectFromHermes()` real: lê `~/.hermes/cron/state/<date>/bloco-*.md` + `gateway_state.json`; demo fallback só sem state files |
| `deploy/nexus-status-collector.service.example` | NOVO | Template systemd espelhando AI collector |
| `api/ai/ingest.ts` | MOD | Comentário deprecation no fallback RPC `PGRST202` |
| `middleware.ts` | NOVO | Vercel Edge Middleware: CORS allowlist + token-bucket 60req/min/IP com `ipAddress()` |
| `playwright.config.ts` | NOVO | Config Playwright (chromium, port 5173, webServer auto) |
| `e2e/overview.spec.ts` | NOVO | 5 specs e2e mínimos (Overview, topbar, theme, rotas /ai-infrastructure + /routine) |
| `package.json` | MOD | `test:e2e` script; devDeps `@playwright/test` + deps `@vercel/functions` |

## Revertidos pelo linter (não alterar)

- `api/_shared/http.ts` — const inline preservadas (api não usa alias `@`)
- `scripts/ai-collector.ts` — `"0.4.0"` hardcoded preservado
- `vitest.config.ts` — thresholds não suportados nessa versão do vitest
- `.github/workflows/ci.yml` — step check:shapes não adicionado
- `scripts/check-shapes.ts` — JSDoc "hard-fail" preservado como era

## Para rodar (quando classifier voltar)

```bash
cd ~/Área\ de\ trabalho/dev/nexus-public

# 1. Validar testes (75 existentes + sem regressão)
npm test

# 2. Validar build (mock e api)
npm run build
VITE_DATA_MODE=api npm run build

# 3. Validar typecheck
npm run typecheck

# 4. Commit
git add -A
git commit -m "feat: status collector real, rate limit/CORS, Playwright e2e, import.meta.env fix

- system-status-collector: collectFromHermes reads real state files
- middleware.ts: Edge CORS allowlist + rate limit 60/min/IP
- playwright.config.ts + e2e/overview.spec.ts
- nexus-api: defensivo import.meta.env para vitest
- ai-enums.ts: single source AI_PERIODS/AI_METRICS
- schemas.ts: Zod enums + ROUTINE_TIMEZONE from constant"

# 5. Instalar Playwright
npx playwright install chromium

# 6. E2E
npm run test:e2e

# 7. Push
git push
```

## Notas

- `import.meta.env` fix: defensivo — `typeof import.meta !== "undefined"` antes de ler env. Não afeta prod/dev (Vite sempre injeta).
- handlers.test.ts foi removido pelo linter — recriar se necessário com `http.get("*..."` matcher pattern.
- axe-core (vitest-axe) não instalado — William pode adicionar depois com `npm i -D vitest-axe`.
